import { StopSpinnerAfterViewInit } from './stop-spinner-after-view-init';

describe('StopSpinnerAfterViewInit', () => {
	it('should create an instance', () => {
		expect(new StopSpinnerAfterViewInit()).toBeTruthy();
	});
});
