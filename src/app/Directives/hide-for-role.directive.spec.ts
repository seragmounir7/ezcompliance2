import { HideForRoleDirective } from './hide-for-role.directive';

describe('HideForRoleDirective', () => {
	it('should create an instance', () => {
		const directive = new HideForRoleDirective();
		expect(directive).toBeTruthy();
	});
});
