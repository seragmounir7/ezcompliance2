import { CheckImgExtPipe } from './check-img-ext.pipe';

describe('CheckImgExtPipe', () => {
	it('create an instance', () => {
		const pipe = new CheckImgExtPipe();
		expect(pipe).toBeTruthy();
	});
});
